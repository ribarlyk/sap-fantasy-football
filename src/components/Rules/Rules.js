import "./Rules.css";
import { motion } from "framer-motion"

export default function Rules() {
    return (
        <motion.div className="rules-container">
            <h1>Rules</h1>
        </motion.div>
    );
}
